{
  "_comment": "Digest of the migration chain reviewed for outbound behaviour. Re-pin only after reviewing the diff.",
  "sha256": "baf39b3d2c40700b3bebf5c2fc540b5415c614131b59d2b3c5817620dbe84fa8",
  "files": 626,
  "pinned_at": "2026-08-10",
  "reviews": [
    {
      "from": {
        "sha256": "3d87a2a95000198b20acdf47fb12fae75b648b5521a451359a9238c18982efce",
        "files": 607,
        "pinned_at": "2026-08-07"
      },
      "to": {
        "sha256": "baf39b3d2c40700b3bebf5c2fc540b5415c614131b59d2b3c5817620dbe84fa8",
        "files": 626
      },
      "added": 19,
      "range": "20261113100000_u1a_academy_player_memberships .. 20261202100000_u2_identity_worker_cron_inert",
      "units": "U1a, U1b, U1c prereqs 1-4 (#641-#646, already merged to main) and U2 incl. slice A (#647)",
      "outbound_sweep": {
        "method": "every sanitizer sweep pattern applied to all 19 added migrations: cron.schedule, cron.alter_job, net.http_post, net.http_request_queue, supabase_functions.http_request, extensions.http_, dblink, CREATE FOREIGN TABLE/SERVER, schedule_in_database, vault.*, CREATE EXTENSION, pg_read_, COPY",
        "migrations_with_outbound_capability": [
          "20261202100000_u2_identity_worker_cron_inert.sql"
        ],
        "new_outbound_capable_functions": "none — no added migration defines a function that reaches net.http_post, supabase_functions.http_request, dblink or extensions.http_*, so reviewed-outbound-functions.tsv is unchanged",
        "new_extensions": "none — no added migration issues CREATE EXTENSION, so reviewed-extensions.tsv is unchanged",
        "false_positives_checked": "the only COPY hit (20261117100000) is the word 'copy' inside a prose comment, not a COPY command"
      },
      "why_the_sanitized_clone_stays_outbound_inert": [
        "THE ACTUAL PROOF, and it is simpler than the stub-execution story an earlier draft of this record told: on a sanitized rehearsal target the migration never runs past its first statement. Its opening guard is `IF NOT EXISTS (SELECT 1 FROM pg_extension WHERE extname = 'pg_cron') THEN RETURN`, and sql/platform_stub.sql deliberately creates the cron/net SCHEMAS and functions WITHOUT registering a pg_extension row for pg_cron (it only checks that the real extension is absent). So the DO block raises a notice and returns before it reads Vault, before it takes the advisory lock, before it looks up any job, and before cron.schedule is ever called. Nothing is stored and nothing is scheduled.",
        "Because it returns there, the net.http_post text inside the $cmd$ literal is never even written to cron.job on a clone. Even if it were, that call sits inside a string: applying a migration stores such a command, it does not execute it.",
        "Belt and braces beneath that: the sanitizer neutralises CREATE EXTENSION pg_cron/pg_net so the real extensions are never installed, and there is therefore no scheduler process on a rehearsal target to tick a stored command at all.",
        "And beneath that again: the stub's own cron.schedule INSERTs with active = false and its cron.alter_job hard-codes active = false, so even a migration that tried to arm a job could not.",
        "NOT relied upon, and corrected here because an earlier draft of this record claimed it: the stub's cron.job.jobname is GLOBALLY unique, not unique per (jobname, username) as real pg_cron is. This migration's owner-scoped lookup is therefore not exercised on a sanitized target and no claim about it should rest on one. That property is proven instead by src/test/identityCronInert.realpg.test.ts, whose stub models the real (jobname, username) uniqueness."
      ],
      "inventories_updated": [
        "reviewed-cron-jobs.tsv — added notification-identity-worker, outbound=yes (the column classifies the stored command, not whether the job is armed; it ships INACTIVE)",
        "reviewed-outbound-functions.tsv — unchanged, no new outbound-capable functions",
        "reviewed-extensions.tsv — unchanged, no new extensions"
      ]
    }
  ]
}
