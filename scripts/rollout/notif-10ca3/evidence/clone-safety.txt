$ bash scripts/rollout/notif-10ca3/verify/clone-safety-test.sh

== the withdrawn fence path is unreachable ==
  PASS  clone-source-quiesce REFUSES (exit 1)
  PASS  …without ever invoking psql — the refusal precedes any connection
  PASS  …and without invoking the Supabase CLI
  PASS  …and wrote no window evidence
  PASS  …and says the command is withdrawn
  PASS  …citing the measured production result
  PASS  …and points at the ADR
  PASS  …and names the supported replacement
  PASS  …and states plainly that nothing was touched
  PASS  no 'seal' subcommand exists
  PASS  no 'arm' subcommand exists
  PASS  no 'fence' subcommand exists
  PASS  no 'marker' subcommand exists
== recovery survives, and only with explicit window evidence ==
  PASS  resume refuses with NO recorded window
  PASS  resume works for a window whose nonce is on file (recovery preserved)
  PASS  abandon refuses without --nonce
  PASS  abandon works when the operator names the nonce
  PASS  a malformed nonce is refused
== the rehearsal target must be a real, distinct, EMPTY project ==
  PASS  an empty, inert, correctly-identified target is accepted
  PASS  a PRODUCTION url is refused as a rehearsal target
  PASS  a CLONE_REF equal to production is refused
  PASS  a malformed CLONE_REF is refused
  PASS  a target with non-zero E_CRON is refused BEFORE anything is loaded
  PASS  a target with non-zero E_NETQ is refused BEFORE anything is loaded
  PASS  a target with non-zero E_VAULT is refused BEFORE anything is loaded
  PASS  a target with non-zero E_HOOK is refused BEFORE anything is loaded
  PASS  a target with non-zero E_OUTTRIG is refused BEFORE anything is loaded
  PASS  a target with non-zero E_FDW is refused BEFORE anything is loaded
  PASS  a target with non-zero E_AUTH is refused BEFORE anything is loaded
== the baseline is synthetic, measured, and only the affected tables ==
  PASS  an UNMEASURED scale file is refused (invented rows give an invented timing)
  PASS  …and no data was loaded
  PASS  a measured scale builds the baseline
  PASS  …schema comes from a db push (main), not from the #615 pin
  PASS  …and the log says so, so the migrations under test are applied separately
  PASS  …every schedule the migrations created was deactivated
  PASS  …and a pristine baseline fingerprint was recorded
  PASS  a baseline containing a NON-synthetic address is refused
  PASS  …and not recorded
TABLES email_address_state,email_delivery_events,invoices,trainer_profiles
  PASS  the generator writes ONLY the two tables #615 locks plus the minimum FK parent graph (email_address_state,email_delivery_events,invoices,trainer_profiles)
  PASS  …and every invoice field is synthetic (SYN- numbering, placeholder name)
  PASS  …and the trainer parent's only literal value is 'synthetic'
  PASS  …filling exactly the NOT NULL columns the live schema declares, not a hard-coded guess
  PASS  every generated address uses the reserved, undeliverable example.invalid TLD
  PASS  the generator is deterministic — no executable Math.random (repeatable rehearsals)
-- the build is inert WHILE it runs, not only afterwards --
  PASS  the inert cron/pg_net shims are installed BEFORE the schema build (14 migrations on main call cron.schedule)
  PASS  the inert stand-in is part of the shared build path
  PASS  the migration source is SANITIZED (the chain installs pg_cron/pg_net; stand-ins alone would collide)
  PASS  …and the push runs against the sanitized directory, not the repo's
  PASS  …and stand-in, sanitize and push are textually in that order, so it cannot drift
  PASS  a target with REAL pg_cron/pg_net is refused (stand-ins cannot shadow them)
  PASS  a chain that cannot be sanitized aborts the build (fail closed on the unknown)
  PASS  …including a chain that merely MOVED — main changing forces a re-review
  PASS  …and nothing was pushed
  PASS  …and no schema was pushed to it
-- reset is a REAL rebuild, not a row reload --
  PASS  reset succeeds
  PASS  …by WIPING schema, shims and the migration ledger (reloading rows cannot undo a migration)
  PASS  …and re-pushing the full migration chain
  PASS  …with the shims installed first, again
  PASS  …through the SAME build path, so a reset cannot drift from the build
  PASS  the wipe empties the migration ledger (leaving it would apply a SUFFIX next time)
  PASS  …and drops the schema, so columns/functions/constraints really are gone
-- recovery is offered ONLY for a target this tooling part-built --
  PASS  a non-empty target that this tooling did NOT build is refused
  PASS  …and --recover is explicitly NOT offered for it (destroying someone else's project is the wrong answer)
  PASS  …while the ORIGINAL diagnostic is still shown (the failing check is named), not replaced by a generic message
  PASS  a clone-IDENTITY failure fails as itself
  PASS  …and never recommends --recover (auth/connectivity/identity are not recoverable by wiping)
-- a stub that dies PART-WAY leaves nothing behind --
  PASS  a stand-in install that dies part-way fails the build
  PASS  …and left NOTHING behind (the artifact is one transaction), so a plain retry works
  PASS  the stand-in artifact is wrapped in a single transaction
  PASS  …and the recovery marker is created INSIDE it, early, so a later failure is still recoverable
-- a FIRST build that fails part-way is recoverable, and only explicitly --
  PASS  a generator failure fails the first build
  PASS  …and records NO fingerprint, so nothing false is left behind
  PASS  …but the schema DID land, so the target is no longer empty (the stuck state)
  PASS  a plain retry is refused — the target is not empty
  PASS  …and the refusal names the recovery command instead of leaving the operator stuck
  PASS  a plain reset is refused too — there is no baseline to verify against
  PASS  …and it too points at --recover
  PASS  clone-reset-baseline --recover wipes, rebuilds and records a pristine baseline
  PASS  …and the fingerprint now exists
  PASS  --recover is REFUSED once a baseline exists (it discards a target; that must stay deliberate)
== a rehearsal cannot start from a drifted or failed-migration baseline ==
  PASS  an untouched target matches its recorded baseline
  PASS  a target whose SHAPE changed (a part-applied migration) is refused
  PASS  a target whose ROW COUNT changed is refused
  PASS  reset also fails while the target still reports drift
  PASS  reset restores the pristine baseline WITHOUT a production snapshot
  PASS  …by reloading synthetic data
  PASS  with no recorded baseline, verification refuses
== the clone gate is wired into every target-touching command ==
  PASS  clone-push is gated
  PASS  clone-make-prefix is gated
  PASS  verify-clone is gated
  PASS  the gate proves INERTNESS (no marker, no fence — there is no provenance question)
  PASS  …and that the target still matches the pristine baseline
== MUTANTS ==
  PASS  MUTANT (refusal downgraded to a warning) lets the withdrawn command proceed — a hard die, not a warn, is load-bearing
  PASS  MUTANT (empty-project check removed) accepts a target holding Vault secrets — proving inertness before loading is load-bearing
  PASS  MUTANT (measured-scale check removed) builds a baseline from placeholder row counts — an invented scale would be reported as a real timing
  PASS  MUTANT (synthetic assertion removed) records a baseline containing a real-looking address — the PII guard is load-bearing
  PASS  MUTANT (baseline check removed from the gate) rehearses against a drifted target — a failed migration could leave a reusable false green
  PASS  MUTANT (shim AND deactivation removed) builds the schema with nothing keeping cron inert — one of the two is load-bearing
  PASS  MUTANT (wipe removed from reset) leaves the previous rehearsal's schema, shims and ledger in place — the wipe is what makes a reset pristine
================  99 passed, 0 failed  ================
