$ node scripts/rollout/notif-10ca3/verify/clone-safety-pg.mjs

[1] the fence really blocks every write path to cron.job
  PASS  the seal artifact runs clean on a real Postgres
  PASS  three fence triggers exist (two on cron.job, one on the pg_net queue)
  PASS  fence rejects direct INSERT (what cron.schedule does)  — 42501
  PASS  fence rejects direct UPDATE  — 42501
  PASS  fence rejects direct DELETE (what cron.unschedule does)  — 42501
  PASS  fence rejects TRUNCATE  — 42501
  PASS  fence rejects cron.alter_job (SECURITY DEFINER path)  — 42501
  PASS  fence rejects a zero-row UPDATE  — 42501
  PASS  fence rejects a direct pg_net queue INSERT  — 42501
  PASS  fence rejects net.http_post() (the pg_net enqueue path)  — 42501
  PASS  no cron row was created by any blocked attempt
  PASS  no outbound request was queued by any blocked attempt
[2] the seal is atomic: a failure leaves nothing behind
  PASS  seal REFUSES with a non-empty pg_net queue  — ASSERT FAILED: pg_net request queue is EMPTY (actual=1 expec
  PASS  …no rollout_clone schema was left behind
  PASS  …no fence trigger was left behind on either table
  PASS  …every job is still ACTIVE (nothing was paused)
  PASS  seal REFUSES when the live configuration is not the reviewed one
  PASS  …and again left nothing behind
[3] in-flight runs: every NON-TERMINAL pg_cron state blocks arming
  PASS  arm REFUSES with a run in status 'starting'
  PASS  arm REFUSES with a run in status 'connecting'
  PASS  arm REFUSES with a run in status 'sending'
  PASS  arm REFUSES with a run in status 'running'
  PASS  arm REFUSES with a run in status NULL
  PASS  arm REFUSES with a run in status 'a-future-state-nobody-anticipated'
  PASS  arm proceeds with only terminal 'succeeded' runs
  PASS  arm proceeds with only terminal 'failed' runs
  PASS  arm REFUSES with a queued pg_net request (defence behind the fence)
[4] cron.log_run must be on, or quiescence is unprovable
  PASS  arm REFUSES when cron.log_run is off (a zero count would be a false green)  — arm: cron.log_run is "off" — job_run_details is not populated, so a zero in-flig
  PASS  …and proceeds once it is on
[5] the ACL matrix is enforced source-side, before the commit
  PASS  anon has no USAGE, no EXECUTE on the fence function and no table access
  PASS  authenticated has no USAGE, no EXECUTE on the fence function and no table access
  PASS  service_role has no USAGE, no EXECUTE on the fence function and no table access
  PASS  PUBLIC cannot EXECUTE the fence function
  PASS  a leaked grant is caught by the SAME matrix at the next checkpoint  — arm: role anon can USE the rollout_clone schema
[6] resume: exact restoration, and atomic on failure
  PASS  resume REFUSES when a command changed under the same id and name  — ASSERT FAILED: cron configuration matches the sealed snapshot EXACTLY 
  PASS  …and rolled back completely: all three fences still installed
  PASS  …marker still present
  PASS  …every job still inactive
  PASS  inside the window: marker present AND zero active jobs
  PASS  after resume: marker gone
  PASS  after resume: fence gone
  PASS  after resume: EXACT prior active states restored (1 and 9 active, 10 inactive)
[7] a SECOND session never observes a marker beside active cron
  PASS  mid-transaction, another session cannot read cron.job at all (it blocks on the lock)  — 57014
  PASS  …and still sees the PRE state: the marker is present  — marker=1
  PASS  after the commit it sees the POST state (marker gone, prior actives restored)  — marker=0 active=2
  PASS  so no committed state carries a valid marker beside active cron
[7b] pg_net is FROZEN across the armed window, from another session
  PASS  another session cannot enqueue via a direct INSERT after the arm  — 42501
  PASS  another session cannot enqueue via net.http_post() after the arm  — 42501
  PASS  the queue is still empty, so nothing can cross into a clone
  PASS  the clone gate re-proves the queue fence inside the clone
  PASS  resume drops the queue fence atomically with everything else
  PASS  …and pg_net can enqueue again once the window is closed
[8] lifecycle: no implicit reuse, and real run-level exclusion
  PASS  a second seal is REFUSED while a window is open  — ASSERT FAILED: no prior sealed window exists (if this fails:
  PASS  …and the original marker is untouched
  PASS  a concurrent run holding the advisory lock blocks a second seal  — ASSERT FAILED: no other clone-safety quiesce/resume is runni
[9] the clone gate, executed against a real restored state
  PASS  a state inside the armed, fenced window passes the clone gate
  PASS  a different run's nonce is refused
  PASS  a clone with a run in 'sending' is refused (not just 'running')
  PASS  a clone missing ONLY the pg_net queue fence is refused
  PASS  a clone whose fences are gone is refused (restore point outside the window)
================  60 passed, 0 failed  ================
