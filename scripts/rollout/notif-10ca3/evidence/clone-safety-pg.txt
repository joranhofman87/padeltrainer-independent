$ node scripts/rollout/notif-10ca3/verify/clone-safety-pg.mjs

[1] empty-project proof on a genuinely empty database
  PASS  empty_project_check passes on a pristine target
  PASS  empty_project_check REFUSES a target holding a cron job
  PASS  empty_project_check REFUSES a target holding a queued pg_net request
  PASS  empty_project_check REFUSES a target holding a recorded pg_net response
  PASS  empty_project_check REFUSES a target holding a Vault secret
  PASS  empty_project_check REFUSES a target holding an FDW server
[2] outbound triggers are caught, including nested call paths
  PASS  a trigger that only INDIRECTLY reaches net.http_* is caught
[3] schedule deactivation and the loaded-target inertness gate
  PASS  the inertness gate REFUSES while jobs are active
  PASS  clone_deactivate_schedules runs clean
  PASS  …and every job is now inactive
  PASS  …and no job was unscheduled (the schedule is part of what is verified)
  PASS  the inertness gate now PASSES
[4] real schema + synthetic baseline + the real #615 migrations
  PASS  the synthetic generator loads a production-scale baseline  — BYTES email_address_state generated=942080 measured=900000 drift=4.7% tolerance=
  PASS  email_address_state loaded to the configured scale
  PASS  email_delivery_events loaded to the configured scale
  PASS  every address is on the reserved undeliverable TLD — no customer identifier exists in the target
  PASS  ONLY the two affected tables received scale data (notification_outbox untouched)
  PASS  the baseline fingerprint covers shape, size, distribution and bloat
  PASS  …and asserts every address is synthetic
[5] the real #615 migrations: timing and ACCESS EXCLUSIVE behaviour
  PASS  the three pinned migrations apply to the synthetic baseline  — 246 ms at 4k/12k rows
  PASS  the generated column exists after the rewrite
  PASS  the migration CHANGES the shape fingerprint, so a migrated target can never pass as pristine  — SHAPE 2a1ccb -> SHAPE c51528
  PASS  …and no timing/lock evidence carries an address or reason string
[6] the real migration under contention: abort, and ZERO delta
  PASS  the REAL migration ABORTS on lock_timeout while a reader holds the table  — 55P03
  PASS  …leaving ZERO schema delta (shape fingerprint unchanged)  — SHAPE 2a1ccb vs SHAPE 2a1ccb
  PASS  …zero function delta  — 52 -> 52
  PASS  …and the generated column was NOT added
  PASS  …and zero ledger delta (nothing was recorded as applied)  — ledger=0
  PASS  uncontended, the same migration APPLIES — the abort was contention, not broken SQL  — 99 ms over 2k rows
================  29 passed, 0 failed  ================
