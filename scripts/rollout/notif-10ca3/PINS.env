# Single source of the reviewed + tested PR head SHAs. Both the CI harness
# (verify/chain.mjs applies the #615 migrations at PR615_SHA) and the operator
# script (run-rollout.sh merges ONLY these exact heads via
# `gh pr merge --match-head-commit`) read this file, so CI evidence and the
# deployed commit cannot drift apart.
PR615_SHA=2b247f43afe8ff73d32f44f4f58c3d9918471c77
PR616_SHA=6c34f67915a31b80e1dcc30bf586a9435c73300a
