

# Position "Create" Tab Between Manage and Trainer Hours

## Change

| File | Change |
|------|--------|
| `src/pages/academy/AcademyCalendar.tsx` | Insert the new `TabsTrigger` for `"create"` between the `"manage"` and `"hours"` triggers (after line 639, before line 640). Tab order becomes: Overview → Open Spots → Manage → **+ Create** → Trainer Hours. |

Single line insertion — everything else from the approved plan stays the same.

