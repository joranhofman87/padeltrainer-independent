

# Default Lesson Type: Group of 4

## Change
In `src/components/cycles/CycleApplicationForm.tsx`, line 184, change the default `lesson_types` from `['group3']` to `['group4']` so the "group of 4" option is pre-selected on the registration form.

### `src/components/cycles/CycleApplicationForm.tsx`
- Line 184: `lesson_types: ['group3'] as string[]` → `lesson_types: ['group4'] as string[]`

